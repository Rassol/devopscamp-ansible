---
- name: Install http
  apt:
    name: "{{ item }}"
    state: present
  with_items:
   - python-pip

- name: Copy the code from repository
  git: 
   repo: "{{ repository }}"
   dest: "/var/www/html/code"

- name: Install specified python requirements.
  pip:
   requirements: "/var/www/html/code/{{ repo_name }}/requirements.txt"
 
- name: Insert a line at the end of a file.
  lineinfile:
    path: "/etc/hosts"
    line: "{{ redis.ansible_ssh_host }} redis" 